
num y is 11.
num x is y + 10 * 50.

fn add_one(num x):num -> x + x.




# TODO: parse these:
# add_x(1)(2).
# (add_x(1))(2).

###
# Fibonacci
fn fun_fib(0):num -> 1.
fn fun_fib(1):num -> 1.
fn fun_fib(num x):num -> 
	fun_fib(x-1) + fun_fib(x-2).

fun_fib(2).
###


# returns a function that adds the given number (a) to the input (x).
###
fn get_adder(num a):(num):num ->
	fn(num x):num ->
		x + a.

plus2 is get_adder(2). # should be (num x):num -> x + 2.
X is plus2(2). # should be 4
###

# Example of ambiguous call
# fn ex(num a, 2) -> a.
# fn ex(1, num b) -> b.
# ex(1,2).